#:kivy 1.10.0

<CameraView@BoxLayout>:
	orientation: 'vertical'

	on_touch_down: setattr(camera, 'play', not camera.play) and print(camera.play)

	Camera:
		id: camera
		#resolution: (800, 480)
		resolution: (640, 480)
		index: 0
		play: True
